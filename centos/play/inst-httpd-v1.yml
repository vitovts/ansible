---
# --- Playbook for install httpd centos
- name: Install default Apache Web Server
  hosts: centos
  become: yes


  tasks:
    - name: Install Apache Web Server
      yum:
        name: httpd
        state: latest

    - name: Start Aoache and Enable it on the every boot
      service:
        name: httpd
        state: started
        enabled: yes